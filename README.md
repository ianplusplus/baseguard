# baseguard
Lightweight Linux HIDS that baselines and monitors processes, ports, cron jobs, systemd units, binaries, kernel modules, SUID files, SSH config, and user accounts — with email alerts via AWS SES.
